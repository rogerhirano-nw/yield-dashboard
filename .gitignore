.env
*.db
*.csv
# deal_health publishes the date-stamped weekly CSV; allow that one path.
!reports/*.csv
# Bombora taxonomy seed files committed to data/.
!data/bombora_*.csv
# Locally-generated email previews — never committed.
reports/*.html
__pycache__/
.streamlit/secrets.toml
.DS_Store

# Licensed Newsweek font binaries are dropped into static/fonts/ locally
# but must never be committed (see static/fonts/README.md).
static/fonts/*
!static/fonts/README.md

# Claude Code: track shared settings, ignore local/session state
.claude/*
!.claude/settings.json
!.claude/hooks
!.claude/hooks/**
