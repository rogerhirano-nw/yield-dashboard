# newsweek.com ads.txt — audit & sellers.json cross-check

**Date:** 2026-06-18 · **OWNERDOMAIN:** newsweek.com · **Records:** 183

Every row was validated against the publishing exchange's live `sellers.json`
on three axes:
1. **Existence** — the `seller_id` is actually published by that ad system.
2. **Type agreement** — `DIRECT` ⇒ `seller_type` PUBLISHER/BOTH; `RESELLER` ⇒ INTERMEDIARY/BOTH.
3. **Ownership** — `DIRECT` seats resolve to `domain: newsweek.com` (or are confidential).

**Result: 183 / 183 rows cross-checked.** 15 lines were corrected (see below).
A handful of items need a human/partner decision — they are *not* fixable by
editing ads.txt and were left in place, flagged.

---

## Changes applied to `ads_clean.txt` (15)

### A. Formatting / case normalization (8)
Uppercased `reltype`, normalized comma spacing to `, `. No id/type/count change.
- `infolinks.com, 3445427` — `direct` → `DIRECT`
- `pubmatic.com, 156872` — lowercased+unspaced row normalized
- `openx.com, 543174347` — normalized
- `rubiconproject.com, 20322` — normalized
- `aps.amazon.com, de948cab-…` — normalized
- `conversantmedia.com, 40881` — added missing space before cert id
- (xandr `3251` and indexexchange `191306` also normalized — see B)

### B. Certification-authority id added / corrected (4)
Only where another row for the **same ad system** in this file already
establishes the correct TAG-ID (internal inconsistency), or the value was wrong.
- `themediagrid.com, GODNC4` — cert `9fac4a4a87c2a44f` → **`35d5010d7789b49d`** (matches the file's other 4 themediagrid rows)
- `xandr.com, 3251` — add **`f5ab79cb980f11d1`** (xandr `4009` / all appnexus rows carry it)
- `xandr.com, 9830` — add **`f5ab79cb980f11d1`**
- `indexexchange.com, 191306` — add **`50b1c356f2c5c8fc`** (other IX rows carry it)
- `smartadserver.com, 4111` — add **`060d053dcf45cbf3`** (other SAS rows carry it)
- `smartadserver.com, 3050` — add **`060d053dcf45cbf3`**

### C. DIRECT ↔ RESELLER corrections — confirmed against sellers.json (3)
Each contradicted the exchange's own published `seller_type`. **These are
relationship declarations — sanity-check against your contracts; revert any you
know to be intentional.**
- `getmediamx.com, 1221186` — **RESELLER → DIRECT** · sellers.json: `PUBLISHER`, `newsweek.com` (a seat you own ⇒ DIRECT)
- `openx.com, 561588636` — **DIRECT → RESELLER** · sellers.json: `INTERMEDIARY`, `google.com`, "Newsweek Publishing LLC (via EB)" (Exchange-Bidding path)
- `inmobi.com, 4bc8811…2172` — **DIRECT → RESELLER** · sellers.json: `INTERMEDIARY`, `google.com`, "newsweek-OB" (Open-Bidding path)
  - *(its sibling `inmobi.com, 973c9f74…2017` stays DIRECT — sellers.json: `PUBLISHER`, `newsweek.com` ✓)*

---

## Needs a decision — left unchanged in the file

### Unauthorized seats (not in the exchange's sellers.json — currently no-ops)
- **`media.net, 8CUW47PH3, DIRECT`** — not present in media.net's `sellers.json`
  (2,459 sellers, TAG-ID `818f58666cabc936`). media.net lists Newsweek's real
  direct seats as **`8CUFR4KCY`** and **`8CUR8943F`** (both already in your file).
  → Likely a stale/typo'd id. **Remove it, or ask media.net to confirm/issue it.**
- **`infolinks.com, 3445427, DIRECT`** — not present in infolinks' `sellers.json`
  (1,082 sellers; no newsweek-owned seat at all). → **Confirm the correct account
  id with infolinks** (don't drop blindly — it's your only infolinks row).

### Reseller routed through a third party (valid, but confirm authorization)
- **`media.net, 8CUTQ396X, RESELLER`** — present, but `seller_type: PUBLISHER`,
  `domain: mgid.com` (owned by **MGID**). Valid as a reseller path (Newsweek → MGID
  → media.net). **Confirm MGID is an authorized reseller of Newsweek inventory.**

### Inconsistent Open-Bidding variant (recommended flip — couldn't confirm via sellers.json)
- **`onetag.com, 770185590958b74-OB, DIRECT`** — every other `-OB` / `-eb` Open-Bidding
  variant in your file is **RESELLER** (smartadserver `5429-OB`, sharethrough `5429-OB`,
  lijit `265361-eb`), and the base seat `770185590958b74` is already declared DIRECT.
  → **Recommend `-OB` → RESELLER.** (`-OB` manifests aren't listed separately in
  sellers.json, so this is inferred from the file's own pattern, not confirmed — hence
  not auto-applied.)

---

## Optional enhancements (not errors)
- **`yieldmo.com, 3690863157472863021, RESELLER`** is valid (sellers.json:
  `INTERMEDIARY`, "Newsweek.com (via Google EB)"). It's a 3-field row; yieldmo
  publishes TAG-ID **`6a92c77cfc3d2258`** — append it to match your other 4-field rows.
- **Published OB/direct seats you don't currently declare** (add only if you want
  those paths authorized):
  - `media.net, 8CU6G3OPF` (INTERMEDIARY, "Newsweek … via OB") → as RESELLER
  - `yieldmo.com, 3538697789661716823` & `3606772164256014558` (PUBLISHER, newsweek.com)
    → as DIRECT, *if* you transact with Yieldmo directly (not only via Google OB)
- **`newsweek.com, 1, DIRECT`** — self-referential owner entry; verify it's intentional
  and that `newsweek.com/sellers.json` lists seller id `1`.

---

## Spot-clean confirmations (representative)
- **PubMatic** — all 20 rows valid (3 DIRECT seats resolve to PUBLISHER + newsweek.com; 17 RESELLER → INTERMEDIARY/BOTH).
- **OpenX** — 5 DIRECT seats: `537110299 / 540274391 / 540562163 / 565034601` all PUBLISHER+newsweek ✓; only `561588636` was the EB mislabel (flipped).
- **onlinemediasolutions.com, 21186, DIRECT** — PUBLISHER + newsweek.com; cert `b3868b187e4b6402` matches OMS's published TAG-ID ✓.
- **media.net** — `8CUFR4KCY` / `8CUR8943F` DIRECT ✓; `8CUWNDO6S` RESELLER → Minute Media (BOTH) ✓.
